
void nubes() {
  tint(255, 200);
  //py = random (width);
  image (nube1, 0, 0); 
  image (nube2, 0, 0);
  //image (nube, 0, py+400);
  //image (nube, 0, py-400);
  //image (nube, 0, py-800);
  
}
